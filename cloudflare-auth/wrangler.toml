# 🐱 猫猫的小破烂 - Cloudflare Workers 配置文件

name = "maomaomz-auth"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV 命名空间绑定
[[kv_namespaces]]
binding = "CODES"
id = "736f830f14394b1b18c315a2d6b9db31b"

# 环境变量（管理员密钥）
[vars]
# 注意：实际密钥应该在 Cloudflare Dashboard 中以 Secret 形式设置
# 这里只是示例
ADMIN_SECRET = "your-super-secret-admin-key-here"

# 🔄 定时任务（Cron Triggers）
# 每天 UTC 时间 16:00（北京时间 00:00）自动更新授权码
[triggers]
# 每小时触发一次，由代码检查是否到达用户配置的更新时间
crons = ["0 * * * *"]
